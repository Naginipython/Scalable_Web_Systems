import express from 'express';

const router = express.Router();

// DELETE user/anime (Deletes an entry)
router.delete('/user/:anime', (req, res) => {
    
});

// DELETE user (deletes user)

export default router;
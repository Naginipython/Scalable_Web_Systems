import express from 'express';

const router = express.Router();

// PUT user/anime (updates an entry)
router.put('/:user/anime', (req, res) => {
    
});

// PUT user (updates for user)

export default router;